import './bootstrap';

import Alpine from 'alpinejs';

// Make Alpine globally accessible via the `window` object (optional but useful for debugging or global access)
window.Alpine = Alpine;

// Initialize Alpine.js and activate its reactive functionality on the page
Alpine.start();
